const QuotationStatus = Object.freeze({
    APPROVED: 'aprobado',
    REJECTED_BY_CLIENT: 'rechazado por cliente',
    DISCARDED: 'descartada',
    PENDING: 'Pendiente'
});


module.exports = {
    QuotationStatus
}